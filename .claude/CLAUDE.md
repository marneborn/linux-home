# Claude Instructions

<!-- Global instructions applied to all projects. -->
For multi-line shell commands, avoid backslash line continuations (`\`) — put the pipe (`|`) at the end of the line instead to avoid trailing space issues.

