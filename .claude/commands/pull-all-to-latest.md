---
description: Pull all core and utility repos to latest main, report dirty ones
---

Run the pull-all-to-latest script to update all Glade repos:

```bash
bash ~/home-git/bin/pull-all-to-latest.sh
```

Show the output directly to the user. Do not add any extra commentary — the script output speaks for itself.
